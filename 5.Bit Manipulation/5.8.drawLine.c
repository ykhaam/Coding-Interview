#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

/*
5.8 선 그리기
흑백 모니터 화면은 하나의 바이트 배열에 저장되는데,
인접한 픽셀 여덟개를 한 바이트에 묶어서 저장한다.
화면의 폭은 w이며 w는 8로 나누어 떨어진다.
따라서, 어떤 바이트도 두 행에 걸치지 않는다! 물론, 화면 높이는
배열 길이와 화면 폭을 통해 유도해 낼 수 있다. 이때 (x1,y)에서 (x2,y)까지
수평선을 그려주는 함수를 작성하라. 메서드 용법은 다음과 같다.

drawLine(byte[] screen, int width, int x1, int x2, int y)
*/
void EnableBit(int x, int y) {
	
}

int main() {

}